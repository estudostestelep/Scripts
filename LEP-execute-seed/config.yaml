server:
 #url: https://lep-system-341885235510.us-central1.run.app
 url: http://localhost:8080
 timeout: 30

auth:
  organization_name: "Osteria"
  fallback_email: "pablo@lep.com"
  fallback_password: "senha123"
  auto_email: true

seed:
  file: seed-osteria.json
  stop_on_error: false
  parallel: true

logging:
  level: debug
  show_payloads: true
